<% include partials/header %>
<%- messages('message', locals) %>

<head>

    <!--Load the AJAX API-->
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">

      // Load the Visualization API and the corechart package.
      google.charts.load('current', {'packages':['corechart']});

      // Set a callback to run when the Google Visualization API is loaded.
      google.charts.setOnLoadCallback(drawChart);

      // Callback that creates and populates a data table,instantiates the chart, passes in the data and draws it.
      function drawChart() {

        // Create the data table
        var data = new google.visualization.DataTable();
        data.addColumn('string', 'HaveGivenUsers');
        data.addColumn('number', 'HaveNotGivenUsers');


        var awardJSON = <%- JSON.stringify(awards); %>
        //var awardData = JSON.parse(awardJSON);

        var awardData = new Array();

        var awardHaveItem = new Array();
        awardHaveItem.push( 'Have Given Users');
        awardHaveItem.push( awardJSON[0].HaveGivenUsers);
        awardData.push(awardHaveItem);

        var awardHaveNotItem = new Array();
        awardHaveNotItem.push( 'Have Not Given Users');
        awardHaveNotItem.push( awardJSON[0].HaveNotGivenUsers);
        awardData.push(awardHaveNotItem);

        //var awardChartJSON = JSON.stringify(awardData);

        data.addRows(awardData);

        // Set chart options
        var options = {'title':'Awards Created by Department',
                       'width':800,
                       'height':500};

        // Instantiate and draw our chart, passing in some options.
        var chart = new google.visualization.PieChart(document.getElementById('chart_div'));
        chart.draw(data, options);
      }
    </script>
  </head> 

<body>
        <h1>Happy Paws Employee Portal</h1><br>

        <h4>Analytics & Reporting</h4><br>
        
        <p>Filter Award Data By:</p>
        <a class="btn btn-custom1" href="/users/admin/analytics" role="button">Users</a>
        <a class="btn btn-custom1" href="/users/admin/analyticsDept" role="button">Department</a>
        <a class="btn btn-custom1" href="/users/admin/analyticsAwardType" role="button">Award Type</a>
        <a class="btn btn-custom1" href="/users/admin/analyticsViewAllAwards" role="button">View All Awards</a>

    <!-- <Div that holds the bar chart -->
    <div id="chart_div"></div>
    
    <p>TODO:  Insert Pie Chart for Depts that have created awards vs Depts that have never created an award.</p>   

  </body>

<br>


<a class="btn btn-custom1 btn-lg" href="/users/admin/adminmenu" role="button">Back to Admin Main Menu</a><br><br>

<% include partials/footer %>